html
  head
    title="Home"
  body
  include navigation.pug
  h1="Home"
  p Welcome to the Home page.
  if sensorData
    
    each item in sensorData
      p sensor URI: #{item.sensorUri}
      p broker Uri: #{item.brokerUri}
      if item.subscribeTopics || item.publishTopics
        form(action="/subscribe" method="post" enctype="multipart/form-data")
          if item.subscribeTopics
            for topic in item.subscribeTopics
              ul(id=topic) subscribe topic: #{topic}
            
          if item.publishTopics
            for topic in item.publishTopics
              ul(id=topic) publish topic: {topic}
            
          button(type="submit") Subscribe
      //p key: #{item.key}